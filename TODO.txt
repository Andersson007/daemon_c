1. Add usage some logging lib in Logger proc
2. Implement log level for debug (and others) by .cfg file
3. Use not only absolute path
4. Check log priority of -l is higher than of cfg
5. Pass Log ts format string through .cfg file
6. Find another way to get nested field from struct
