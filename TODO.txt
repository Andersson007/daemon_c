General:
- Find another way to get nested field from struct


Logging related:
- Implement log level for debug (and others) by .cfg file
- Check log priority of -l is higher than of cfg
- Pass Log ts format string through .cfg file
- Use not only absolute path while passing paths by CLI args


Config file related
- Use not only absolute path while passing paths by CLI args


Wont implement:
- Add usage some logging lib in Logger proc (decided to do it by my own)
